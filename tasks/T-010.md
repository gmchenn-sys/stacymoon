# T-010 · F6 记录页接 Agent 周/月健康汇总（P0，等 Jamie 部署后自动生效）

> status: done

## 背景
Jamie A4 已实现 `GET https://stacymoon.online/v1/conversations/{user_id}/health-summary?period=week|month`（契约 §4.1）。**注意：生产暂未部署（实测 404），本卡按契约实现 + 静默容错，Jamie 部署后无需改前端。**

## 实现要求
1. `history.html`（T-008 之后的版本）列表上方加"Stacy 周总结"卡片：
   - `fetch(.../v1/conversations/${encodeURIComponent(code)}/health-summary?period=week)`，`code` = `localStorage.stacy_invite_code`；
   - 响应结构未知，做宽容解析：优先 `data.summary`（string），否则 `data.text`，否则整个 body 是 string 就直接用；解析不出→隐藏卡片；
   - 请求失败/404/超时（8s AbortController）→ 整卡隐藏，console.warn 一条即可；
   - 周/月切换小 tab（week/month 两个按钮，点击重拉）。
2. 结果按天缓存 `stacy_health_summary` {date, period, text}，命中不重调（同 stacy_daily_brief 模式）。

## 涉及文件
- `history.html`

## 禁区
- 不动打卡表单（T-008 的成果）和打卡列表逻辑
- 禁止 git add -A

## 验收标准
```bash
grep -n 'health-summary' history.html
grep -n 'stacy_health_summary' history.html
```
手测：当前 404 下页面无报错、卡片不显示、其余功能正常。

## 完成动作
`git add history.html tasks/ && git commit -m "T-010: 记录页接周月健康汇总(F6)"`，agmsg 报 architect。
