# T-004 · 语音字幕联调适配（Christine 侧已上线文本帧）

## 背景
Christine 已完成语音侧改造：STT interim/final、Agent SSE token/done、call_start/call_end 都会以 **JSON 文本帧**通过语音 WebSocket 下发，Agent 请求已切 `/chat/stream`（reply_mode=voice / channel=voice / call_id）。

前端基础设施在 f1ca371 已就位（app.js:378 `handleVoiceTextFrame` 分发器、app.js:701 string/ArrayBuffer 分流、done 后 saveLog）。**本卡是联调适配，不是重写。**

## 目标
确保 Christine 实际发的事件名/字段与前端分发器完全兼容，字幕与落库跑通。

## 实现要求
1. `app.js` `handleVoiceTextFrame` 增加事件名别名容错（对齐口径是"transcript/token/done"）：
   - `case 'transcript':` 并入 `user_transcript` 分支；
   - final 标志兼容 `msg.final === true || msg.is_final === true`；
   - 文本字段已有 `text || content` 容错，保持。
2. `vtCompleteTurn` 的字段容错检查：bot 文本兼容 `response / bot_text / content`。
3. `call_end` 兜底落库路径确认：通话中途断线（ws onclose）也要触发一次 `vtCompleteTurn` 兜底（检查现有 onclose 是否已调用，没有则补上）。
4. 更新 `docs/VOICE_WS_EVENTS.md`：状态从"前端提案 v0.1 待 Christine 确认"改为"v1.0 已上线（2026-07-12）"，并把别名（transcript/is_final 等）写进事件表。

## 涉及文件
- `app.js`、`docs/VOICE_WS_EVENTS.md`

## 禁区
- 不动音频播放链路（BotAudioPlayer / worklet.js）
- 不动 saveLog 的写库字段结构
- interim 一律不落库（现有逻辑不许破坏）

## 验收标准
```bash
grep -n "case 'transcript'" app.js
grep -n "is_final" app.js
grep -n "v1.0" docs/VOICE_WS_EVENTS.md
```
模拟验收（浏览器 console，聊天页执行）：依次 `handleVoiceTextFrame(JSON.stringify({type:'call_start',call_id:'test-1'}))`、`({type:'transcript',text:'昨晚没睡好',final:false})`、`({type:'transcript',text:'昨晚没睡好',final:true})`、`({type:'token',content:'听起来'})`、`({type:'done',response:'听起来昨晚不太好'})` → 页面出现用户气泡（interim 转正常）+ bot 气泡，且 localStorage stacy_logs 新增一条 channel=voice 记录。真实通话联调由用户手测。

## 完成动作
`git add app.js docs/VOICE_WS_EVENTS.md tasks/ && git commit -m "T-004: 语音字幕事件别名容错+协议文档v1.0"`（禁止 git add -A），agmsg 报 `T-004 done, commit <sha>, <一句话>`（团队 stacymoon）。
